package widget

// Test-only handles onto tuning that has no business being API, so the
// measurement harness can sweep values through the real code path.
var (
	VelocityTau    = &velocityTau
	VelocityWindow = &velocityWindow
)
