pluginManagement {
    repositories { google(); mavenCentral(); gradlePluginPortal() }
}
dependencyResolutionManagement {
    repositories { google(); mavenCentral() }
}
// The identifier, not the display name: gradle reserves / \ : < > " ? * |
// in a project name, and the label users see comes from the manifest.
rootProject.name = "{{.ProjectName}}"
include ':app'
