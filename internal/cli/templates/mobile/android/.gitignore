# Gradle / Android build outputs
.gradle/
build/
app/build/
app/.cxx/
local.properties

# gomobile-bind artifacts (regenerated by `gophics run -p android`)
app/libs/*.aar
app/libs/*.jar

# Keep the Gradle wrapper (gradle/wrapper/*, gradlew, gradlew.bat) tracked.
