# Markdown Guide

The renderer supports a practical CommonMark subset.

## Inline

You can write **bold**, *italic*, and `inline code`. Links look like
[this](https://example.com), and [[Welcome]] jumps to another note.

## Lists

- First item
- Second item with `code`
- Third item linking to [[Ideas]]

## Code blocks

```
func main() {
    app.Run(ui.Root(), ui.Config())
}
```

That is enough to keep readable notes. Head back to [[Welcome]].
